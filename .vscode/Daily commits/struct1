#include <stdio.h>

struct Gene
{
    char name[30];
    int length;
};

int main()
{
    struct Gene gene1;

    strcpy(gene1.name, "BRCA1");
    gene1.length = 1863;

    printf("Gene Name: %s\n", gene1.name);
    printf("Gene Length: %d\n", gene1.length);

    return 0;
}